import {Link,useLocation} from 'react-router-dom';import {BookOpen,BrainCircuit,LayoutDashboard,LogOut,ShieldCheck,GraduationCap} from 'lucide-react';import {useAuth} from '../context/AuthContext';
export default function Layout({children}:any){const {user,logout}=useAuth();const loc=useLocation();const links=user?.role==='admin'?[['/admin','Admin',ShieldCheck],['/courses','Catalog',BookOpen]]:user?.role==='instructor'?[['/instructor','Instructor',LayoutDashboard],['/courses','Catalog',BookOpen]]:[['/dashboard','Dashboard',LayoutDashboard],['/courses','Courses',BookOpen]];return <div className="app"><aside><div className="brand"><BrainCircuit/> VertexLearn <span>AI</span></div><nav>{links.map(([to,label,I]:any)=><Link className={loc.pathname.startsWith(to)?'active':''} to={to} key={to}><I size={18}/>{label}</Link>)}</nav><div className="profile"><div className="avatar">{user?.full_name?.[0]||'V'}</div><div><b>{user?.full_name}</b><small>{user?.role}</small></div><button onClick={logout}><LogOut size={17}/></button></div></aside><main>{children}</main></div>}
