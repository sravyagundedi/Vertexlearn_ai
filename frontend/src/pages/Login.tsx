import {FormEvent,useState} from 'react';import {useNavigate} from 'react-router-dom';import {useAuth} from '../context/AuthContext';import {BrainCircuit,LogIn} from 'lucide-react';
export default function Login(){const {login}=useAuth();const nav=useNavigate();const [email,setEmail]=useState('student@vertexlearn.local');const [password,setPassword]=useState('Password123!');const [err,setErr]=useState('');async function submit(e:FormEvent){e.preventDefault();try{await login({email,password});nav('/dashboard')}catch(e:any){setErr(e.response?.data?.error?.message||'Login failed')}}return <div className="auth"><div className="auth-card"><div className="brand big"><BrainCircuit/> VertexLearn <span>AI</span></div><p className="muted">Personalized learning, grounded in your course material.</p><form onSubmit={submit}><label>Email<input value={email} onChange={e=>setEmail(e.target.value)}/></label><label>Password<input type="password" value={password} onChange={e=>setPassword(e.target.value)}/></label>{err&&<div className="error">{err}</div>}<button className="primary"><LogIn size={18}/> Sign in</button></form><div className="demo">Demo: student / instructor / admin accounts all use <b>Password123!</b></div></div></div>}
