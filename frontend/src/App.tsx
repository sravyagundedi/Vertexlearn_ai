import {BrowserRouter,Routes,Route,Navigate} from 'react-router-dom';import {AuthProvider,useAuth} from './context/AuthContext';import Layout from './components/Layout';import Login from './pages/Login';import Dashboard from './pages/Dashboard';import Courses from './pages/Courses';import Instructor from './pages/Instructor';import Admin from './pages/Admin';import CourseDetails from './pages/CourseDetails';import NotFound from './pages/NotFound';import './styles.css';
function Private({children,roles}:any){const {user,loading}=useAuth();if(loading)return <div className="loading">Loading VertexLearn…</div>;if(!user)return <Navigate to="/login" replace/>;if(roles&&!roles.includes(user.role))return <Navigate to={user.role==='student'?'/dashboard':user.role==='instructor'?'/instructor':'/admin'} replace/>;return <Layout>{children}</Layout>}
export default function App(){return <AuthProvider><BrowserRouter><Routes><Route path="/login" element={<Login/>}/><Route path="/dashboard" element={<Private roles={['student']}><Dashboard/></Private>}/><Route path="/courses" element={<Private><Courses/></Private>}/><Route path="/courses/:id" element={<Private><CourseDetails/></Private>}/><Route path="/instructor" element={<Private roles={['instructor','admin']}><Instructor/></Private>}/><Route path="/admin" element={<Private roles={['admin']}><Admin/></Private>}/><Route path="/" element={<Navigate to="/dashboard" replace/>}/><Route path="*" element={<NotFound/>}/></Routes></BrowserRouter></AuthProvider>}
